"""use python to read and write files. Let's start with text files
files are opened with open()"""
myfile=open("myfilename.txt") #the argument is the path to the file.
#if the file is in the current workimg directory, only neeed to use its name
"""the second argument to open() specifies the mode
"r": read mode, which is the default.
"w": write mode, rewriting the contents of a file.
"a": append mode, adding new content to the end of the file.
"b": binary mode, non-text files (such as image and sound files)."""
# write mode
open("filename.txt", "w")

# read mode
open("filename.txt", "r")
open("filename.txt")

# binary write mode
open("filename.txt", "wb") #modes can be combined
"""close files with variable.close()"""
file = open("filename.txt", "w")
# do stuff to the file
file.close()

"""read files using variable.read"""
file = open("/usercode/files/books.txt")
cont = file.read()
print(cont)
file.close()
"""use bytes as a read() argument to read a section of a file. Each ASCII character is 1 byte"""
file = open("/usercode/files/books.txt")
print(file.read(5)) #reads the first 5 characters
print(file.read(7)) #then the next 7
print(file.read()) #then the rest of the file.
file.close()

"""use variable.readlines() to return each line in the file"""
file = open("/usercode/files/books.txt")

for line in file.readlines():
    print(line)
    
file.close()
#In the output, the lines are separated by blank lines
"""You need to make a program to read the given number of characters of a file.
Take a number N as input and output the first N characters of the books.txt file."""
N=int(input())
file = open("/usercode/files/books.txt")
#your code goes here
print(file.read(N))
file.close()
"""variable.write to write files. If the file ar=lready exists, usng .write will
erase its conents"""

"""file = open("newfile.txt", "w")
file.write("This has been written to a file")
file.close()

file = open("newfile.txt", "r")
print(file.read())
file.close()"""

"""use "a" to add to a file."""
"""file = open("/usercode/files/books.txt", "a")

file.write("\nThe Da Vinci Code")
file.close()

file = open("/usercode/files/books.txt", "r")
print(file.read())
file.close()"""

"""the write method, =open(".txt", "w") returns the number of bytes written to a file, if successful"""
#To write something other than a string, it needs to be converted to a string first
"""
Take a number N as input and write the numbers 1 to N to the file "numbers.txt", each number on a separate line.

Sample Input
4

Sample Output
1
2
3
4

The given code reads the content of the file and outputs it.
"""
a = int(input())

file = open("numbers.txt", "w+")
#your code goes here
for i in range(1,a+1):
    
    file.write("\n" + str(i))
file.close()

f = open("numbers.txt", "r")
print(f.read())
f.close()
#make sure files are closed using try and finally
try:
  f = open("/usercode/files/books.txt")
  cont = f.read()
  print(cont)
finally:
 f.close() #file is always closed, even if there is an error

#use "with" statements to create a temporary variable within the block and ensure the file is closed
with open("/usercode/files/books.txt") as f:
  print(f.read()) #the file closes automatically at the end of the with statement, even if there are errors

"""
You are given a books.txt file, which includes book titles, each on a separate line.
Create a program to output how many words each title contains, in the following format:
Line 1: 3 words
Line 2: 5 words
...

Make sure to match the above mentioned format in the output."""
with open("/usercode/files/books.txt") as f:
   #your code goes here
   s=0
   for line in f.readlines():
      s +=1
      a=1
      for i in line:
         if i == " ":
            a+=1

      print("Line " + str(s)+ ": "+str(a)+" words")

"""You are given a file named "books.txt" with book titles, each on a separate line.
To encode the book titles you need to take the first letters of each word in the title and combine them.
For example, for the book title "Game of Thrones" the encoded version should be "GoT".

Complete the program to read the book title from the file and output the encoded versions, each on a new line."""
file = open("/usercode/files/books.txt", "r")
for i in file.readlines():
    x=i.split(" ")
    finale = ""
    for j in x:
        c=str(j)
        finale+=c[0]
    print(finale)

file.close()
